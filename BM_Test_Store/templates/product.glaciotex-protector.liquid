{% include 'product-details' %}

{% comment %}
{% if product.id == 6768480583785 %}
 {% section 'pdp-terry-protector-icon-grid' %}
 {% section 'terry-protector-product-detail' %}
 {% section 'pdp-accordion-terry-protector' %}
{% else %}
{% endif %}
{% endcomment %}
 {% section 'pdp-protector-glaciotex-icon-grid' %}
  {% comment %}
   {% section 'protector-video' %}
  {% endcomment %}
 {% section 'pdp-protector-glaciotex-features' %}
 <section id="review-section" class="ypreviews">
  <div class="ypreviews-wrap">
    <h2 class="reviews_heading" id="reviews"> {{ product.title }} Reviews</h2>
		<div class="yotpo-widget-instance" data-yotpo-instance-id="544318" data-yotpo-product-id="{{product.id}}" data-yotpo-name="{{ product.title | escape }}" data-yotpo-url="{{ shop.url }}{{ product.url }}" data-yotpo-image-url="{{ product.featured_image | product_img_url: "large" | prepend: "https:" | replace: '?', '%3F' | replace: '&','%26'}}" data-yotpo-price="{{ product.price | divided_by: 100.00 }}" data-yotpo-currency="{{ shop.currency }}" data-yotpo-description="{{ product.description | escape }}"></div>
	</div>
</section>
 {% section 'pdp-accordion-glaciotex-protector' %}

{% section 'g-p-faq-component' %}
  {% comment %}
{% section 'protector-standard-content' %}
  
{% section 'protector-standard-content-2' %}
  {% endcomment %}

{% if product.id == 6768480583785 %}
{% else %}
  {% comment %}
<section id="review-section" class="ypreviews">
  <div class="ypreviews-wrap">
    <h2 class="reviews_heading" id="reviews">Bear Protector Reviews</h2>
		<div class="yotpo yotpo-main-widget"
		    data-product-id="{{ product.id }}"
		    data-name="{{ product.title | escape }}"
		    data-url="{{ shop.url }}{{ product.url }}"
		    data-image-url="{{ product.featured_image | product_img_url: "large" |replace: '?', '%3F' | replace: '&','%26'}}"
		    data-description="{{ product.description | escape }}">
		</div>
	</div>
</section>
    {% endcomment %}
{% endif %}

{% include 'schema' %}